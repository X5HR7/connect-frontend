import { FC } from 'react';
import styles from './ServerSidebar.module.scss';

const ServerSidebar: FC = () => {
	return <div className={styles.sidebar}>server sidebar</div>;
};

export { ServerSidebar };
